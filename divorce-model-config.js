// A real probability is disabled until a longitudinal model has independent
// external validation and an explicit deployment approval. The research
// training pipeline writes a candidate file with deploymentApproved: false.
window.CPQ_DIVORCE_MODEL = null;
