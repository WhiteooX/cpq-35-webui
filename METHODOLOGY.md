# 方法学与实现审计

## 产品边界

核心产品是 CPQ-35 双人自陈量表 WebUI。宏观情感持续时间与 SPAFF-informed 9 维整体评级是独立扩展，不属于 CPQ，不用于修正、验证或解释 CPQ 分数。

## 与旧版本相比的关键修正

1. **题面恢复到 CPQ-35 的原始行为内容。** 旧版本若干自拟题改变了题意，尤其是威胁、人格攻击、身体攻击、内疚／受伤和向他人求助等题位。
2. **标准加总取代平均分。** Revised scoring 的正式分量表值为题目加总；平均分仅适用于某些研究中为比较不同题数分量表而进行的转换。
3. **拒绝部分作答计分。** 旧版本会对只回答一部分题目的分量表求平均，容易把不完整数据显示成正式结果。
4. **修正双人方向配对。** A 要求／B 回避必须把 A 的 SD/PW 与 B 的 PD/SW 并排；反方向则配对 A 的 PD/SW 与 B 的 SD/PW。
5. **移除自创阈值。** 不再使用 4.5、6.5、差异 2 分或负向比例 50% 自动生成关系判断。
6. **不再制造一个 CPQ 关系总分。** 两位伴侣的分量表报告保持独立；差值只表示报告不一致程度。
7. **观察模块改用持续时间。** 旧版本用点击事件次数计算“情感分布”，无法代表状态持续比例；新版按毫秒持续时间汇总。
8. **不再把自拟观察冒充 SPAFF。** 新增 9 维整体评级时明确标为 SPAFF-informed，并保留来源、训练和一致性限制。
9. **拒绝伪概率。** 横断面 CPQ 只产生一个明确标注的描述性研究指数；只有固定期限法律离婚模型通过独立外部验证和人工部署审批后，概率接口才会启用。

## 计分定义

对每位作答者分别计算：

- `CC = Q2 + Q6 + Q8 + Q23 + Q25 + Q27 + (10-Q1) + (10-Q24) + (10-Q26)`
- `SD/PW = Q3 + Q9 + Q11 + Q13 + Q17 + Q19 + Q32`
- `PD/SW = Q4 + Q10 + Q12 + Q14 + Q18 + Q20 + Q33`

未进入 revised scoring 的 12 个题位仍保留在 CPQ-35 中，不应从正式作答流程删除。

## 解释约束

- 高 CC 表示作答者报告更多建设性沟通。
- 高 SD/PW 或 PD/SW 表示作答者报告更多相应方向的要求／回避模式。
- 当前默认配置不提供人群百分位、临床截点、诊断、暴力风险等级或关系结局概率。
- 两位伴侣分数不同可能来自观察角度、记忆、议题差异或真实行为差异；绝对差不是一个已验证的新分量表。

## 沟通压力研究指数

为满足界面上的即时汇总需求，同时避免把任意公式包装成离婚概率，本项目新增一个 0–100 的透明派生指标：

- `lowConstructive = 100 × (81 − mean(A.CC, B.CC)) / 72`
- `demandWithdraw = 100 × (mean(A.SD/PW, A.PD/SW, B.SD/PW, B.PD/SW) − 7) / 56`
- `communicationStrain = (lowConstructive + demandWithdraw) / 2`

两部分各占 50%，只因为这是最简单且可审计的预设，而不是因为研究已经证明该权重最优。0–100 是理论范围标准化，不是百分位；这个组合没有独立信度、结构效度、常模、截点或预测效度，因此界面必须始终称为“研究指数”，不得改称 CPQ 总分、风险等级或离婚概率。

## 经校准的离婚概率接口

网站默认加载空配置，因此显示“不可计算”。启用数字输出必须同时满足：

1. 结局严格定义为某个正整数月数内是否发生法律离婚；
2. 开发数据按情侣而非个人拆分为训练、校准和测试集；
3. 系数、训练集标准化参数和概率校准参数完整；
4. 独立外部队列验证状态为通过；
5. 研究负责人完成人工部署审批。

`research/train_divorce_model.py` 提供可复现的逻辑回归、独立校准和性能报告管线，但它有意不会自动批准部署。若随访时间不一或存在删失，当前固定期限二元模型不适用，应改用预先规定的生存分析管线。即使模型启用，概率也只适用于模型卡所列人群、期限和结局，不能外推成未婚伴侣分手概率或个体确定性结论。

## 尚需实证研究回答的问题

- 中文对照文本的翻译等值性、测量不变性和本土常模。
- 非异性婚姻、非婚伴侣和不同文化群体中的结构稳定性。
- 在线作答相对纸笔版本的测量等值性。
- 两人同机顺序作答与真正独立作答之间是否存在情境效应。
- 情感观察扩展的编码员训练与编码员间一致性。
- CSI、IMS、ECR-R 等新增模块的中文翻译等值性，以及基线、1、3、6、12 个月的纵向效度。
- 关系质量变化与分居／分手属于不同结局，需要分别建模和外部验证。

更完整的量表矩阵、SPAFF 升级条件和纵向建模建议见 `LITERATURE_REVIEW.md`。
